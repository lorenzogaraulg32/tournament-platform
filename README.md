### Initial SetUp

First of all, for each microservice, copy the application.yml.example and remove .example
After that you need to configure some fields, like secret key for jwt and username and password for db.